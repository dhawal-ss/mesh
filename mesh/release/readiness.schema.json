{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:mesh:release:readiness-schema:v2",
  "title": "Mesh production readiness ledger",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "ledgerId", "sourceCommit", "sourceTreeHash", "updatedAt", "gates"],
  "properties": {
    "schemaVersion": { "const": 2 },
    "ledgerId": { "const": "mesh-production-readiness" },
    "sourceCommit": {
      "description": "Commit that anchors the source snapshot. A dirty local snapshot is identified by this commit plus sourceTreeHash and is never clean-source release evidence.",
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "sourceTreeHash": {
      "description": "Git tree hash of the complete source snapshot represented by the ledger.",
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "updatedAt": { "type": "string", "format": "date-time" },
    "gates": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/gate" }
    }
  },
  "$defs": {
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "testedCommit",
        "testedTreeHash",
        "command",
        "artifactPath",
        "environment",
        "collectedAt",
        "expiresAt"
      ],
      "properties": {
        "testedCommit": {
          "description": "Commit checked out when this evidence was collected.",
          "type": ["string", "null"],
          "pattern": "^[0-9a-f]{40}$"
        },
        "testedTreeHash": {
          "description": "Git tree hash of the exact files exercised by the evidence.",
          "type": ["string", "null"],
          "pattern": "^[0-9a-f]{40}$"
        },
        "command": { "type": ["string", "null"] },
        "artifactPath": { "type": ["string", "null"] },
        "artifactUri": { "type": ["string", "null"], "format": "uri" },
        "artifactSha256": {
          "type": ["string", "null"],
          "pattern": "^[0-9a-f]{64}$"
        },
        "artifactRunAttempt": {
          "description": "Exact GitHub Actions rerun attempt that produced a protected artifact.",
          "type": ["integer", "null"],
          "minimum": 1
        },
        "environment": { "type": ["string", "null"] },
        "collectedAt": { "type": ["string", "null"], "format": "date-time" },
        "expiresAt": { "type": ["string", "null"], "format": "date-time" }
      }
    },
    "waiver": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["approver", "reason", "expiresAt"],
      "properties": {
        "approver": { "type": "string", "minLength": 1 },
        "reason": { "type": "string", "minLength": 1 },
        "expiresAt": { "type": "string", "format": "date-time" }
      }
    },
    "gate": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "milestone",
        "required",
        "releaseStatus",
        "status",
        "evidence",
        "owner",
        "capability",
        "blockReason",
        "nextAction",
        "waiver"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:[.-][a-z0-9]+)*$"
        },
        "milestone": { "enum": ["R0", "R1", "R2", "R3", "R4"] },
        "required": { "type": "boolean" },
        "releaseStatus": { "enum": ["local-pass", "live-pass"] },
        "status": {
          "enum": ["unverified", "local-pass", "live-pass", "blocked", "waived"]
        },
        "evidence": { "$ref": "#/$defs/evidence" },
        "owner": { "type": "string", "minLength": 1 },
        "capability": { "type": "string", "minLength": 1 },
        "blockReason": { "type": ["string", "null"] },
        "nextAction": { "type": ["string", "null"] },
        "waiver": { "$ref": "#/$defs/waiver" }
      }
    }
  }
}
